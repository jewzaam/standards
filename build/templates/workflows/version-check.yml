# Generated By: Claude Code (Claude Opus 4.6)
name: Version Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  version-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history needed for merge-base comparison

      - name: Validate semantic versioning
        run: make version-check
