# Generated By: Claude Code (Claude Opus 4.6)
# Replace <package_name> with your Python package name (e.g., my_tool)

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "<package_name>"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
authors = []
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-mock>=3.0",
    "black>=23.0",
    "flake8>=6.0",
    "mypy>=1.11",
    "mutmut>=3.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["<package_name>*"]

[tool.mutmut]
paths_to_mutate = ["<package_name>/"]
